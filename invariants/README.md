# Decision Structure Invariants

This module documents structural properties that persist across domains when decisions are decomposed correctly.

These are not heuristics. They are invariants observed across cases.
